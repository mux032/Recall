<?xml version="1.0" encoding="utf-8"?>
<backup-rules>
    <!-- Exclude AI models and database from backup -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="models" />
    <exclude domain="file" path="cache" />
</backup-rules>
